import { FormatTxtPipe } from './format-txt.pipe';

describe('FormatTxtPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTxtPipe();
    expect(pipe).toBeTruthy();
  });
});
